# SOLAR Linear Probe — Dependencies
# Install: pip install -r linear_probe/requirements.txt

# Core (already in project requirements.txt)
torch>=2.3.0
transformers>=4.44.0
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0

# Quantization (for running large models on consumer GPUs)
bitsandbytes>=0.43.0
accelerate>=0.34.0

# Analysis & figures
matplotlib>=3.8.0
scipy>=1.12.0

# Optional: faster attention
# flash-attn>=2.5.0  # install with: pip install flash-attn --no-build-isolation
